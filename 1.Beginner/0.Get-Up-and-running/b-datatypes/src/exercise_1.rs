// Something's missing. Fix the code so that it compiles.

pub fn run() {
    let is_morning = true;
    if is_morning {
        println!("Good morning!");
    }

    let is_evening = !is_morning; // Finish the rest of this line
    if is_evening {
        println!("Good evening!");
    }
}
